# FungiLib – Image Repository (docs/images)

This folder contains **sample fungal images** used in the FungiLib project for academic and demonstration purposes.

The images stored here are **not part of the backend storage layer**. Instead, they are referenced in the database via **public GitHub raw URLs**, which are saved in Firestore under the field:

